name: ml-foundations

channels:
  - conda-forge

dependencies:
  - python=3.12
  - pip

  - pip:
      - polars>=0.20
      - numpy>=1.26
      - pyarrow>=15
      - matplotlib>=3.8
      - scikit-learn>=1.4
      - catboost>=1.2
      - mlflow>=2.11
      - altair>=5.4
      - plotly>=6.0
      - ipykernel>=6.30